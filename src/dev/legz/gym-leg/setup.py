from setuptools import setup

setup(name='gym_leg',
      version='0.0.1',
      # install_requires=['gym', 'numpy', 'socket', 'transforms3d', 'json']  # And any other dependencies foo needs
      install_requires=['gym']  # And any other dependencies foo needs

)